cv-healthcheck Project Instructions

PROJECT PURPOSE

cv-healthcheck is a Commvault-focused healthcheck, reporting, and operational analysis platform. It supports daily operational reporting, quick health assessments, full environment healthchecks, operational trend analysis, customer reporting, and proactive risk identification.

It draws on Reports Plus / Metrics datasets and reports, Command Center REST APIs, uploaded operational snapshots, and planned S3-based collection pipelines.

ARCHITECTURE PRINCIPLES

Maintain strict separation of responsibilities.

reportsplus/

* Handles Reports Plus inventory and dataset execution
* Handles Metrics server interaction
* No health scoring logic
* No Flask rendering logic

collectors/

* Handles REST API collection from customer environments
* Produces structured collection artifacts
* Must remain reusable and transportable
* Collection must support remote / customer-side execution
* Future collectors may upload artifacts to S3

web/

* Presentation layer only
* Flask routes must remain lightweight
* No business logic in templates
* No health evaluation logic in routes
* Navigation stays simple and product-oriented; keep customer-facing pages separate from internal/development pages
* Do not add broad Dashboard / Admin / Data Sources navigation unless explicitly requested

db/

* Business/application state only
* Customers and engagements currently use raw SQL helpers
* data/app.db is separate from import registries and canonical artifact storage

catalog/

* Local inventory and discovery persistence
* Catalog files are artifacts
* Live APIs remain authoritative

labreadiness/

* Determines whether the environment is suitable for meaningful testing
* Used as gating logic before health-rule development

future rules engine

* Health evaluation
* Correlation logic
* Findings generation
* Scoring
* Recommendations
* Risk interpretation

PROJECT BOUNDARY

cv-topology is a frozen reference/prototype platform. Do not refactor,
reorganize, modernize, or redesign cv-topology unless explicitly requested.
It may be inspected for Commvault API, topology, compliance, Flask, UI, and
database patterns, but active development should stay in cv-healthcheck.

DECISION HIERARCHY

Explicit user instructions override all project documentation.
The user remains the final approval authority for:

* architecture decisions
* roadmap changes
* review sign-off
* release decisions

When sources conflict, authority runs:

1. Explicit user instructions
2. Accepted ADRs
3. PROMPT.txt
4. ROADMAP.md
5. HANDOVER.md
6. CHANGELOG.md

* Newest intentional decision wins.
* ADRs override older planning documents.
* Observations, tool output, and third-party reviews are advisory only — never authoritative over project docs.

DECISION MAKING

Before making architectural changes:

1. Understand existing behavior first.
2. Review related tests.
3. Review relevant ADRs and the roadmap.
4. Prefer extending generic mechanisms over introducing bespoke code.
5. Prefer declarative solutions over procedural ones.
6. Escalate architectural uncertainty before implementing.
7. Validate assumptions against the running system (curl-first / verify-first).
8. Existing code is not proof of intended design; HANDOVER claims are often slightly wrong — grep first.
9. Existing documentation is preferred over chat history; when memory and the docs disagree, the docs win.

Claude Code does the heavy lifting: reads, edits, runs the test suite, validates, drafts the commit message, and pushes. The user reviews and steers; Claude executes.

ENGINEERING RULES

* Preserve clean architecture
* Keep modules focused
* Prefer reusable collectors
* Avoid monolithic route handlers
* Prefer explicit behavior
* Avoid hidden assumptions
* Preserve backward compatibility where practical

DO NOT

* Do not place health logic in Flask routes
* Do not place evaluation logic inside reportsplus clients
* Do not tightly couple collectors to UI
* Do not assume direct CommServe access
* Do not assume the current lab reflects production scale
* Do not silently overwrite existing work

VALIDATION REQUIREMENTS

Before commits:

* python -m compileall src must pass
* python -m pytest must pass
* bash -n scripts/*.sh start.sh must pass

If validation fails, report the exact failing command and failure summary — never claim or pretend success.

Update when appropriate:

* README.md when behavior, stack, or how-to-run changes
* ROADMAP.md when direction changes
* API_MAPPING.md for validated source discoveries

DOCUMENTATION MODEL

The project uses a three-file logging model. Do not reintroduce DEVLOG.md or docs/handover/ — both were retired on 2026-05-25.

* README.md          — what the project is, how to run it, the stack. Mostly static.
* CHANGELOG.md       — backward-looking append-only running log. One entry per session under a date heading (## YYYY-MM-DD). Sections: Added / Changed / Fixed / Removed / Notes. Past entries are never deleted or rewritten — corrections come as new entries.
* HANDOVER.md        — forward-looking "start here" file. Always overwritten, never accumulated. Contains: current branch, latest commit hash, test pass count, what was just completed, what is in-flight, the single recommended next action with file paths and exact step, and pointers to any recent CHANGELOG entries the next session should re-read.

CHANGELOG entries should preserve important findings (root causes of tricky bugs, architectural decisions, gotchas) under the Notes section even when they do not fit Added/Changed/Fixed/Removed.

START OF SESSION

At the start of every new session, read these three files before doing anything else:

* README.md
* HANDOVER.md
* ROADMAP.md

Also check git state first: git status / git branch / git log --oneline -5.

Methodology, workflow stages, and historical lessons live in WORKFLOW.md — read it when doing architecture-sensitive work.

If those three files plus the most recent CHANGELOG entry are not enough to pick up productively, that is a handover defect — improve HANDOVER.md at the end of the session.

SESSION WRAP-UP

At the end of every session:

* validate code: python -m compileall src && python -m pytest
* append a new dated entry to CHANGELOG.md (Added / Changed / Fixed / Removed / Notes)
* overwrite HANDOVER.md with the new forward-looking state
* update ROADMAP.md if direction changed
* update API_MAPPING.md for validated source discoveries
* commit cleanly at every green checkpoint — do not let large amounts of in-flight work accumulate uncommitted, and do not batch unrelated changes
* push to GitHub, then confirm the remote actually moved before declaring done: `git status` shows in-sync (ahead-by-0) and origin/<branch> HEAD matches local. "Pushed" is verified, not asserted.
* summarize completed work clearly
