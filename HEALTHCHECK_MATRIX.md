# HealthCheck Matrix

Operational health evaluation matrix for cv-healthcheck.

This is not the API inventory. Source capabilities are tracked in [API_MAPPING.md](API_MAPPING.md). This matrix answers which health question is being asked, which collected data supports it, which evaluation rule should be applied, and how the result should be reported.

Status values:

- `TODO`: not designed yet
- `DESIGN`: evaluation shape identified, not implemented
- `IMPLEMENTED`: code exists
- `VERIFIED`: validated against real data

| Health Area | Health Question | Required Data | Source Reference | Evaluation Rule | Severity | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Quick HC Report Output | Can the current Quick HC artifacts be assembled into a customer-facing HTML report with subject-level and section-level composition, while keeping internal/debug evidence out of the rendered output? | Current CommCell details, Security Assessment artifact, License Summary artifact, Client Growth metric artifact, Capacity License metric artifact, environment identifiers, and selected section IDs | Quick HC report service; Security Assessment artifact registry; License Summary artifact registry; Metrics artifacts | Current validation checks graceful missing-artifact handling, selected-subject and selected-section rendering, sanitized customer-facing output, Chart.js Client Growth rendering, and License Summary usage visualization where applicable. No scoring, interpretation, or recommendation logic is implemented yet. | INFO | IMPLEMENTED | Current phase is HTML-only report composition over existing artifacts. Customer-facing output intentionally omits artifact paths, dataset GUIDs, HTTP status, and raw/debug extraction fields. |
| Client Growth | Can client growth trend data be queried and interpreted? | MonthStart, Added, Removed, Total | API_MAPPING.md -> Client Growth / Client Usage Trend | Initial rule not implemented. First validation checks whether records are returned and fields are present. | INFO | DESIGN | First healthcheck candidate based on proven Reports Plus dataset access. |
| License Summary | Can License Summary evidence be acquired and normalized consistently across REST, XLSX-recorded REST, HTML export, and CSV export sources? | Canonical license summary artifact with report metadata plus normalized `other_licenses[]`, `agent_feature_licenses[]`, and `workload_summary_sections[]` collections | API_MAPPING.md -> Reports Plus License Summary report 206; License Summary canonical artifact; License Summary artifact registry | Current validation checks source ingestion, generated timestamp parsing, canonical row counts, field normalization, workload summary parsing, registry-backed persistence/read behavior, Quick HC rendering, and customer-facing usage-summary display for capacity-style license rows. No scoring or compliance interpretation is implemented yet. | INFO | IMPLEMENTED | Current phase is acquisition, normalization, provenance capture, persistence, and customer-facing Quick HC rendering only. Missing values remain unset, the UI shows only sections with real returned rows rather than fabricating absent data, and progress-bar visuals are limited to sections where they fit the license shape. |
| Security Assessment | Can Security Assessment evidence be acquired and normalized consistently across REST, HTML export, and CSV export sources? | Canonical checklist artifact with stable section/check/status/remarks/action fields plus source metadata, registry identity, and historical retrieval metadata | API_MAPPING.md -> Security Assessment canonical artifact; Security Assessment artifact registry; Security Assessment history/debug read API; Reports Plus Security Assessment report 336; Security Assessment HTML export import; Security Assessment CSV export import | Current validation checks artifact acquisition, canonical field presence, normalization success, parser resistance to noisy HTML, scoped registry-backed artifact persistence, historical lookup paths, and compatibility fallback behavior. No health scoring rules are implemented yet. | INFO | IMPLEMENTED | Current phase is acquisition/normalization, persistence, and historical retrieval only. Canonical artifact validation and registry tracking exist, but scoring and final health interpretation do not. Remaining open issue: possible REST/source-precedence or stale artifact selection behavior in the render path. |
| Tabular compliance (row-scope rules) | Can data-driven per-row rules over table subjects (`clients`, `server_groups`, `users`) flag rows or counts — empty group, over-allocation (`used > available`), never-logged-in / stale account — as findings, with zero code per rule? | Table-section rows carrying stable column ids + an `id` row key | ADR 0010; `evaluative/row_match.py`; `evaluative/coerce.py`; `db.rules.load_subject_row_rules`; `evaluative/subject_eval.py`; `result_to_artifact` compliance pass | Multi-condition AND predicates (`lt/lte/gt/gte/eq/ne/contains/not_contains/exists/not_exists/between/stale_days`), field-to-field `{ref}`, `emit=per_row`/`count`; registry `kind:row_match` rules bound via `extraction_instructions.evaluative.row_rules:[{ref}]`; a section-level `evaluative.scope` (AND-ed conditions) gates the evaluated population (out-of-scope rows assessed by no rule); every row gets an explicit per-row verdict (good/warning/critical/not_evaluated) baked onto the table rows + previewed by `evaluate_subject`; emits a `<subject>.compliance` FindingsSection | per rule | VERIFIED | **ADR 0010 complete** (Phase 1 core + 2a binding/dry-run + 2b MCP authoring `list_rules`/`save_rule`/`delete_rule`/`evaluate_subject`, with authoring-time validation; the author→evaluate round-trip is regression-guarded after a 2026-06-03 fix where `save_rule`-authored rules weren't persisting `kind` and so were silently un-evaluated). **Verified live**: `server_groups` → 12 empty-group findings incl. both `rommelgroep` (ids 19 & 41) as distinct findings (row_ref=id), each now also carrying `sg_naming_convention` (multi-rule-per-row); `users` → 2 never-logged-in (`last_logged_in==0`). Duplicate *detection* is cross-row, out of `row_match`'s per-row grain. Deferred: summary-scope rules; a count/aggregate kind for duplicate detection; a separate findings store only on the D5 revisit trigger. |
| Job failures | Which jobs are failing and how severe is the failure pattern? | TBD | API_MAPPING.md -> Failed jobs | TBD | TBD | TODO | Requires source mapping before rule design. |
| SLA | Are protected workloads meeting expected SLA targets? | TBD | TBD | TBD | TBD | TODO | Requires source mapping before rule design. |
| Storage capacity | Are storage resources approaching capacity limits? | TBD | API_MAPPING.md -> Capacity/free space | TBD | TBD | TODO | Requires source mapping before rule design. |
| MediaAgent health | Are MediaAgents online and healthy? | TBD | API_MAPPING.md -> MediaAgents | TBD | TBD | TODO | Requires source mapping before rule design. |
| Library health | Are libraries available and healthy? | TBD | API_MAPPING.md -> Libraries | TBD | TBD | TODO | Requires source mapping before rule design. |
| DDB health | Are deduplication databases healthy? | TBD | API_MAPPING.md -> Deduplication DB | TBD | TBD | TODO | Requires source mapping before rule design. |
| Index health | Are indexing components healthy? | TBD | API_MAPPING.md -> Index health | TBD | TBD | TODO | Requires source mapping before rule design. |
| Alert health | Are active alerts present that require attention? | TBD | API_MAPPING.md -> Alerts | TBD | TBD | TODO | Requires source mapping before rule design. |
| Schedule health | Are schedules configured and running as expected? | TBD | API_MAPPING.md -> Schedules | TBD | TBD | TODO | Requires source mapping before rule design. |
| Tenant/company reporting | Can tenant or company scoped health reporting be produced? | TBD | API_MAPPING.md -> Companies / tenants | TBD | TBD | TODO | Requires source mapping before rule design. |
| Security/user review | Are user and security settings ready for review? | TBD | API_MAPPING.md -> Users / security | TBD | TBD | TODO | Requires source mapping before rule design. |
| Network topology | Can topology data be collected for connectivity review? | TBD | API_MAPPING.md -> Network topology | TBD | TBD | TODO | Requires source mapping before rule design. |
| DR backup | Is CommServe DR backup configured and recent? | TBD | API_MAPPING.md -> CommServe DR backup | TBD | TBD | TODO | Requires source mapping before rule design. |
